fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
supabase==2.7.4
sqlalchemy==2.0.36
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
playwright==1.47.0
playwright-stealth==1.0.6
curl-cffi==0.7.3
# LET OP — deze pin bepaalt of de mailagent echte antwoorden schrijft.
# 0.34.2 is van september 2024 en kent `output_config` niet. Toen de mailagent
# op 27-08-2026 die parameter ging meesturen, gooide de oude SDK een TypeError,
# die netjes werd opgevangen — en viel ELK conceptantwoord stil terug op de
# standaard verkoopmail. Rob van Borstelbeer stelde een vraag over
# productvarianten en kreeg video, prijs en platformen terug.
# Bij het verhogen van andere pins: controleer altijd of de SDK de parameters
# kent die de code meestuurt; een te oude SDK faalt hier stil.
anthropic==0.88.0
cloudinary==1.40.0
# Cloudflare R2 spreekt het S3-protocol; hiermee gaan de advertentiefoto's erin.
boto3==1.35.36
apscheduler==3.10.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
aiofiles==24.1.0
stripe==10.12.0
jinja2==3.1.4
beautifulsoup4==4.12.3
google-api-python-client==2.149.0
google-auth==2.35.0
google-ads==26.0.0
google-analytics-data==0.18.12
# Tekent de hero-/deelafbeelding per artikel (backend/content/hero.py). Wordt
# lazy geïmporteerd, dus zonder Pillow publiceert een artikel gewoon zonder
# hero in plaats van te crashen — maar dan mist het wel zijn og:image.
pillow==11.0.0
